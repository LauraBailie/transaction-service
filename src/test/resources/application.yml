spring:
  jpa:
    hibernate:
      ddl-auto: none
      generate-ddl: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    validate-on-migrate: false
    clean-disabled: false